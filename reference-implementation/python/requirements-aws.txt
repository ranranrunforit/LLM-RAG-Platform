# AWS-only additional dependencies for the RAG API container.
# This file is layered on top of requirements.txt — install both:
#   pip install -r requirements.txt -r requirements-aws.txt
#
# boto3 powers the SageMaker runtime client (SageMakerProvider in
# src/llm/sagemaker_provider.py) and the S3 document store.
boto3>=1.34.0
botocore>=1.34.0
