# =============================================================================
# Enterprise LLM Platform with RAG - Python Dependencies
# Project 303 - Reference Implementation
#
# Installation:
#   pip install -r requirements.txt
#
# Google Colab quick install:
#   !pip install -r requirements.txt
# =============================================================================

# ── Core Web Framework ────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.9   # For file uploads

# ── RAG Pipeline (unchanged from original architecture) ───────────────────────
qdrant-client>=1.9.0,<1.11.0  # Must match Qdrant server version (1.9.0); minor diff <= 1
# HuggingFace embedding models (bge-small ~500MB, bge-large ~1.3GB)
sentence-transformers>=2.6.0   # Includes CrossEncoder for reranking
numpy>=1.24.0

# ── GCP / Gemini Pro (primary LLM backend) ────────────────────────────────────
google-generativeai>=0.8.4    # Gemini API (>= 0.8.4 for v1beta/gemini-3 models)
google-cloud-storage>=2.15.0  # GCS for document storage (optional)

# ── HTTP Client (for vLLM API calls) ─────────────────────────────────────────
httpx>=0.27.0

# ── Observability ─────────────────────────────────────────────────────────────
prometheus-client>=0.20.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
tenacity>=8.2.0         # Retry logic for LLM API calls

# =============================================================================
# OPTIONAL DEPENDENCIES
# Uncomment as needed - not required for core functionality
# =============================================================================

# ── PII Detection (full NLP engine, optional - regex fallback is built in) ───
# presidio-analyzer>=2.2.0
# presidio-anonymizer>=2.2.0
# spacy>=3.7.0
# # After install: python -m spacy download en_core_web_lg

# ── Content Moderation (toxic-bert, very heavy ~500MB, off by default) ────────
# transformers>=4.38.0
# torch>=2.0.0            # CPU-only: torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu

# ── Distributed Tracing (optional) ───────────────────────────────────────────
# opentelemetry-sdk>=1.23.0
# opentelemetry-instrumentation-fastapi>=0.44b0
